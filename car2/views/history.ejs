<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <title>สรุปการตรวจสอบย้อนหลัง 5 วัน</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h2>สรุปข้อมูลการตรวจสอบย้อนหลัง 5 วัน</h2>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>วันที่ตรวจสอบ</th>
                    <th>ป้ายทะเบียน</th>
                    <th>หมวดหมู่</th>
                    <th>รายการอุปกรณ์</th>
                </tr>
            </thead>
            <tbody>
                <% data.forEach(row => { %>
                    <tr>
                        <td><%= new Date(row.created_at).toLocaleDateString('th-TH') %></td>
                        <td><%= row.plate_number %></td>
                        <td><%= row.category %></td>
                        <td><%= row.item_name %></td>
                        <td><%= row.quantity %></td>
                </tr>
            </tbody>
        </table>
    </div>
</body>
</html>
